diff --git a/src/conmon.c b/src/conmon.c
index 7f9edf9..0afbb0e 100644
--- a/src/conmon.c
+++ b/src/conmon.c
@@ -35,6 +35,11 @@ static void disconnect_std_streams(int dev_null_r, int dev_null_w)
 		pexit("Failed to dup over stderr");
 }
 
+/* NB: PR_{SET,GET}_CHILD_SUBREAPER were introduced in Linux 3.4 and are not available on CentOS 6. */
+#ifndef PR_SET_CHILD_SUBREAPER
+#define PR_SET_CHILD_SUBREAPER 36
+#endif
+
 int main(int argc, char *argv[])
 {
 	setlocale(LC_ALL, "");
diff --git a/src/conn_sock.c b/src/conn_sock.c
index 7734b0a..6991ede 100644
--- a/src/conn_sock.c
+++ b/src/conn_sock.c
@@ -14,6 +14,11 @@
 #include <sys/un.h>
 #include <sys/stat.h>
 
+/* NB: O_PATH was introduced in Linux 2.6.39 and is not available on CentOS 6. */
+#if !defined(O_PATH)
+#define O_PATH 010000000
+#endif
+
 static gboolean attach_cb(int fd, G_GNUC_UNUSED GIOCondition condition, gpointer user_data);
 static gboolean remote_sock_cb(int fd, GIOCondition condition, gpointer user_data);
 static void init_remote_sock(struct remote_sock_s *sock, struct remote_sock_s *src);
diff --git a/src/ctr_exit.c b/src/ctr_exit.c
index a617df2..65e583a 100644
--- a/src/ctr_exit.c
+++ b/src/ctr_exit.c
@@ -15,6 +15,11 @@
 #include <sys/prctl.h>
 #include <unistd.h>
 
+/* NB: PR_{SET,GET}_CHILD_SUBREAPER were introduced in Linux 3.4 and are not available on CentOS 6. */
+#ifndef PR_SET_CHILD_SUBREAPER
+#define PR_SET_CHILD_SUBREAPER 36
+#endif
+
 volatile pid_t container_pid = -1;
 volatile pid_t create_pid = -1;
 
